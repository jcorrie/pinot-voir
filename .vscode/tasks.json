{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug",
      "program": "/usr/bin/cargo",
      "args": ["run", "${workspaceFolder}/<executable file>"],
      "cwd": "${fileDirname}"
    }
  ]
}
